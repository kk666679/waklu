version: 1
events:
  - name: ProductTracked
    contract: SupplyChainTracker
    fields: [productId, stage, location, timestamp]
  - name: CustodyTransferred
    contract: SupplyChainTracker
    fields: [productId, from, to]
