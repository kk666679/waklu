version: 1
events:
  - name: SupplierVerified
    contract: HalalVerification
    fields: [supplierId, verifier, result, evidenceHash]
  - name: PolicyEvaluated
    contract: HalalVerification
    fields: [entityId, decision, risk]
