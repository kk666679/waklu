version: "2026.08"
networks:
  local:
    provider: http://anvil:8545
    chain_id: 31337
  testnet:
    provider: ${TESTNET_PROVIDER}
    chain_id: 11155111
contracts:
  CertificateRegistry:
    address: ${CERTIFICATE_REGISTRY_ADDRESS}
    abi: contracts/CertificateRegistry.json
  HalalVerification:
    address: ${HALAL_VERIFICATION_ADDRESS}
    abi: contracts/HalalVerification.json
  SupplyChainTracker:
    address: ${SUPPLY_CHAIN_TRACKER_ADDRESS}
    abi: contracts/SupplyChainTracker.json
  EvidenceRegistry:
    address: ${EVIDENCE_REGISTRY_ADDRESS}
    abi: contracts/EvidenceRegistry.json
