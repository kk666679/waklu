id: blockchain-recorder
name: Blockchain Recorder
runtime: python
module: skills.implementations.blockchain_recorder:BlockchainRecorder
description: Anchors evidence hashes and certificate roots on-chain.
inputs:
  hash: string
  metadata: object
outputs:
  txHash: string
  blockNumber: int
integrations:
  blockchain: integrations/blockchain.yaml
eval:
  suite: eval/skills/blockchain-recorder.eval.yaml
