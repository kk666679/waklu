id: alert-dispatcher
name: Alert Dispatcher
runtime: python
module: skills.implementations.alert_dispatcher:AlertDispatcher
description: Dispatches alerts to vendor, compliance, and admin channels.
inputs:
  channel: string
  template: string
  payload: object
outputs:
  delivered: boolean
eval:
  suite: eval/skills/alert-dispatcher.eval.yaml
