# ai-inference runtime dependencies.
# Pinned to the versions resolved on 2026-08-27 for reproducible builds.
# The service runs in Python 3.11 and does NOT use any of the heavy ML
# stacks (torch / sentence-transformers / llama-index). They were
# historically listed but never imported by the actual code paths, so
# they are removed to keep the image small and the build deterministic.

# ── API / web server ─────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# ── HTTP / async ─────────────────────────────────────────────────────────
httpx==0.28.1
tenacity==9.0.0

# ── LLM providers ────────────────────────────────────────────────────────
openai==1.99.9
anthropic==0.45.0

# ── Vector DB / RAG (qdrant-client is the only one actually wired up) ────
qdrant-client==1.15.0

# ── Document processing ──────────────────────────────────────────────────
pypdf==6.1.0
python-docx==1.1.2
openpyxl==3.1.5
beautifulsoup4==4.13.4
lxml==6.0.0
markdown==3.8

# ── Auth / hashing ───────────────────────────────────────────────────────
pyjwt==2.10.1
cryptography==44.0.0

# ── Configuration ────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Observability ────────────────────────────────────────────────────────
structlog==25.1.0
prometheus-client==0.21.1
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-exporter-otlp==1.29.0
python-json-logger==4.0.0
prometheus-fastapi-instrumentator==7.0.0
